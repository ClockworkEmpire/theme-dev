{% comment %}
  Generic page template.
  Used for static content pages like About, Contact, Terms, etc.

  Expected context:
    - page: Dataset record with title, content fields
    - Or accessed via direct template match (e.g., /about → templates/about.liquid)
{% endcomment %}

<div class="bg-page">
  <div class="container section">
    <div class="max-w-3xl mx-auto">
      {% comment %} Page title {% endcomment %}
      {% if page.title %}
        <h1 class="heading-1 mb-8">{{ page.title }}</h1>
      {% endif %}

      {% comment %} Page content {% endcomment %}
      {% if page.content %}
        <div class="card p-8">
          <div class="prose" style="line-height: var(--line-height-relaxed);">
            {{ page.content }}
          </div>
        </div>
      {% else %}
        {% comment %} Fallback for direct template matches without page data {% endcomment %}
        <div class="card p-8">
          <p class="text-secondary">This page has no content yet.</p>
        </div>
      {% endif %}
    </div>
  </div>
</div>
